# Remaining work for the next session (written 2026-09-19 v17)

The v17 branch is `arena/01a0b729-druganalysis` (branched from `main` at `ace3b94`, the v16 merge; v17 merged as PR #32, follow-up review pass merged as PR #33). This session opened the **official FDA series** the project had been missing, reconciled every year 1980–2026 against it, re-probed the Tambocor designation end to end, and committed 12 live primary captures. Open a fresh PR from `arena/01a0b729-druganalysis` and merge it before starting new edits. Repository root: `/home/user/DrugAnalysis`.

## Actions ingestion queued (2026-09-19)

- The Actions fetch manifest includes the pre-1980 blocks (1965–1979), ClinicalTrials.gov Phase 3 windows (2024–2025 and 2028–2029), and pending Yahoo event batches. The runner writes responses under `data/raw/*` with per-request manifests and SHA-256 values.
- After fetches, the workflow runs `scripts/expand_pre1980_decisions.py` only when all three pre-1980 manifests and every year payload are present. It copies only Type 1/Type 1-4 payload facts into `pre1985_fda_decisions.csv`; a missing Type 1 year aborts the write. Applicant identity, indications, tickers, and lineage remain unresolved rather than guessed.
- `scripts/build_stock_snapshots_from_all_events.py` consumes the three pending Yahoo batches idempotently and preserves failed/delisted requests as unavailable rows. It is designed to ingest the queued 1,200 events after Actions commits captures.

## What v17 shipped

- **`data/fda_official_year_series.csv` (85 rows)** — FDA History Office *Summary of NDA Approvals & Receipts, 1938 to the present*, transcribed verbatim (NDAs approved, **NMEs approved**, NDAs received, INDs received) with its footnotes and source list. Capture: `data/raw/source_captures_2026_09_19/fda_history_nda_nme_approvals_1938_2022.json`. **This closes the standing "no official pre-1985 annual NME table was located" limitation** — official counts are now 1980 = 12, 1981 = 27, 1982 = 28, 1983 = 14, 1984 = 22, 1985 = 30.
- **`data/fda_official_series_crosswalk.csv` (47 rows, 1980–2026)** — official vs project, one row per year, with three deltas, a verdict and an evidence note. **17 MATCH** (2004–2012 and 2015–2022). **PROJECT_SHORT_FLAGGED (NME-comparable basis): 1980 (−3), 1981 (−4), 1982 (−3), 1983 (−1), 1984 (−3), 1988 (−1), 2013 (−2).** 2023–2026 = `AWAITING_OFFICIAL_SERIES` (the captured FDA page ends at 2022). Positive deltas 1985–2003 are the Compilation's by-design inclusion of new biologics.
- **`data/pre1985_nme_gap_analysis.csv` (6 rows)** — the 1980–1985 gap, sized and sourced. Key finding: the **43 payload rows with no published class are all strengths/dosage forms of already-marketed ingredients** (extracted per row: furosemide, metronidazole, TMP-SMX, dopamine, nitroglycerin, betamethasone, indomethacin, allopurinol, methyldopa, fentanyl, bupivacaine, tolazamide, ibuprofen, leucovorin, IV electrolytes), so the missing NMEs sit in applications **absent from the ORIG/AP payloads** — the gap class proven for 1985 (Seldane/Protropin/Suprol/Femstat). A live openFDA query returns a **furosemide ORIG-1 of 1968-03-20**, proving the 1983/1984 furosemide "originals" are re-approvals.
- **1983 accuracy correction (important):** the pre-1985 table's 14 rows for 1983 are **not** the official 14 NMEs — 13 are Type 1/1-4 and the 14th (Furosemide Oral Solution, NDA018413) is a non-NME boundary row. Effective 1983 shortfall = **1**. The furosemide row stays as a documented boundary row and must never be counted as an NME.
- **1984 figure confirmed:** FDA's own series says 22 NMEs (the previously "unpinned secondary 22 NMEs" figure), and 142 NDAs approved in 1984 is the largest year in FDA's series. **1985:** the Compilation's 31 rows exceed the official 30 by one; the Compilation's landing page (captured verbatim) states it counts Type 1/1-4 NDAs **plus new biologics**, and Protropin (recombinant somatrem) is the leading candidate on the nature of the product itself rather than on any workbook field (the workbook's NDA/BLA column types all 31 rows 'NDA') — recorded as the **candidate explanation, not asserted**. Baros Effervescent was re-verified live as a Type 1 NDA (NDA018509, 1985-08-07) and ruled out.
- **Era table gained three v17 columns** (`official_fda_nme_count`, `nme_comparable_rows`, `official_series_delta`) plus v17 narrative per year; `data/pre1985_fda_decisions.csv` stayed **byte-identical** on the rerun.
- **Tambocor (D1040) re-probed with live evidence:** Drugs@FDA page → **STANDARD**; committed Compilation workbook parsed directly → **Priority** (so the conflict is between two FDA systems, not a transcription error). The digitized 1985 review PDF is registered by FDA and **five times Wayback-archived (30 Mar 2021 – 30 Mar 2025)**, but the live URL and both raw Wayback replay endpoints returned **HTTP 500** on 2026-09-18 and again on 2026-09-19. A human browser read is the single remaining step; CDER's factual-error address (**CDER.NMENewBiologicApprovals@fda.hhs.gov**) is now recorded on the row.
- **`data/pre1985_primary_captures_index.csv` (12 captures)** — every 2026-09-19 capture indexed to its project rows, verbatim evidence in `data/raw/source_captures_2026_09_19/live_primary_captures_2026_09_19.json`. Precision gained: **Femstat IS in openFDA** (product record, no `submissions` array — that is why the ORIG/AP query cannot see it) while **Suprol is absent outright**; **Seldane and Protropin resolve to empty Drugs@FDA records** (the application number resolves, no products and no approval history published) — the earlier "withdrawn/discontinued" wording is superseded on those rows.
- **`data/pre1980_openfda_probe_2026_09_19.csv`** — the API serves pre-1980 ORIG/AP records, so **a pre-1980 payload fetch is feasible**; it still needs a committed `fetch_jobs` payload (the sandbox has no bulk network).
- **Superseded-claim sweep (same-day review pass):** 390 pre-1998 master rows carried the note clause “no CDER NME year table was published for pre-1998 years” — false once v17 located FDA’s NME year series. The clause is rewritten to “at the time of entry no CDER NME year table had been located” and each row now points at the official series and its year’s crosswalk verdict. Field-level diff proves `notes` was the only column touched; rerun is a no-op. The crosswalk verdict is also now computed on the **NME-comparable** count, so 1983 is correctly flagged short by 1 rather than reading MATCH.
- **Six rows carry additive v17 dated notes, no value changes** (`scripts/annotate_v17_2026_09.py`, idempotent): master D1030, D1038, D1040, D1042, D1047 and non-NME NDA022046.
- **Validator v17 gates:** PASS, 0 errors — pins the official-series values, the 47-row crosswalk shape, the seven short years, the six gap rows and deltas, the 12 captures, the pre-1980 probe, era-table vs gap-table agreement, and the dated notes.
- **Site updated:** Pre-1985 tab gained four panels (official crosswalk, NME gap analysis, capture index, full official year series); the era table shows project rows, official NMEs, NME-comparable rows and Δ side by side.

## Verification already run

```text
python3 scripts/build_official_series_audit_2026_09.py   # 5 tables; aborts on any capture/table drift; idempotent
python3 scripts/annotate_v17_2026_09.py                  # 6 dated notes; rerun = no-op
python3 scripts/expand_pre1985_decisions_v14.py          # era 6 rows w/ v17 columns; decision table byte-identical
python3 scripts/validate_data.py                         # PASS: 0 errors
```

## Next work, in priority order

1. **Name the missing pre-1985 NMEs (1980 ×3, 1981 ×4, 1982 ×3, 1983 ×1, 1984 ×3).** The official series gives the counts; the application list needs the 1989 CDER *Offices of Drug Evaluation: Statistical Report* (pp. 152–199, FDA History Office Files) or the contemporaneous FDA annual report. FDA's page publishes the Historian's contact (john.swann@fda.hhs.gov) and the CDER factual-error address is **CDER.NMENewBiologicApprovals@fda.hhs.gov** — a formal records request is the clean route. Working hypothesis to test, not assume: the missing NMEs are applications absent from Drugs@FDA's ORIG/AP payload (the 1985-proven class), possibly including pre-1997 biologics approved under an NDA.
2. **1988 and 2013 Compilation shortfalls.** The CDER Compilation carries 20 rows for 1988 against the official 21, and 27 for 2013 against the official 29 — the only two such years in 1985–2022. Check the 2013 NDA/BLA calendar-year approvals page and the FDA 2013 novel-approvals list; for 1988 the 1989 typescript is again the source.
3. **Tambocor 1985 review PDF (human, 2 minutes).** Open `https://www.accessdata.fda.gov/drugsatfda_docs/nda/pre96/018830Orig1s000rev.pdf` or the Wayback capture `https://web.archive.org/web/20240929073454/https://www.accessdata.fda.gov/drugsatfda_docs/nda/pre96/018830Orig1s000rev.pdf` and read the review classification block (the 1985 scheme used Type + therapeutic-potential letters). One line of observed text closes the adjudication; do not harmonise without it.
4. **Pre-1980 block (now proven feasible).** Add a `fetch_jobs` payload for e.g. 1975–1979 ORIG/AP through the Actions route, commit the manifests, then apply the same year-by-year treatment. Schema decision still needed: where pre-1980 Type-1/1-4 rows live (the pre-1985 table is 1980–1984 only) and how the era/statistical caveats are stated.
5. **NDA022046 lineage artifact** — still open; needs a 1983 approval letter or Federal Register notice. The v17 captures confirm the broader condition (legacy 1983 originals genuinely publish blank classification/priority) but do not date this application.
6. **141 core-analysis listing-class disagreements** — adjudicate with period 10-K/20-F cover evidence (ADR vs direct: GSK/NVS/RHHBY/AZN/SNY; delisted vs current: SHPG/MDCO/CELG/ALXN). Keep the `CORE_CLASS_BASELINE` ratchet; change only with a primary citation.
7. **ClinicalTrials.gov Phase 3 depth** — the registry still holds the first 2,000 studies of the 2026–2027 window; paginate via the existing fetch-job machinery with replayable payloads/manifests.
8. **Decision-engine quality** — keep approval outcome, stock reaction and phase-transition success as distinct measures; the new official-year crosswalk is the natural spine for per-year completeness scoring on the site.

## Important repository facts

- Branch for all work: `arena/01a0b729-druganalysis` (this session fixed to it); origin `https://github.com/buffedlizard55-lab/DrugAnalysis.git`.
- Single-writer discipline: `build_official_series_audit_2026_09.py` owns the five v17 audit tables; `annotate_v17_2026_09.py` owns the v17 notes; `expand_pre1985_decisions_v14.py` owns the pre-1985 CSV + era CSV; `build_original_non_nme.py` owns the non-NME CSV + year register; `build_focus_year_audit_1980_1985.py` is read-only over all of them. Rerun the scorecard + core + company-scores builders after any master/non-NME change (not needed for v17 — no values changed).
- `data/fda_decisions_master.csv` (1,427 rows, 1985–2026) and `data/pre1985_fda_decisions.csv` (91 rows, 1980–1984) keep their roles. Do not merge them without a deliberate schema/scorecard decision.
- Data-fetch convention: bulk openFDA/ctgov/Yahoo jobs run through GitHub Actions; **the sandbox page-fetch tool DOES work for `accessdata.fda.gov` HTML pages, `drugsatfda_docs` label PDFs and `api.fda.gov` JSON** (use it for one-off primary checks; keep quotes verbatim in `data/raw/source_captures_*/`). SEC EDGAR via the page-fetch tool only (the Actions EDGAR runner returns 403). `archive.org/wayback/available` is frequently rate-limited (HTTP 429) — use the direct `web.archive.org/web/<timestamp>` URL instead. FDA's pre-96 scanned review PDFs have returned HTTP 500 through every automated route so far (live and Wayback) — hand those to a human.
